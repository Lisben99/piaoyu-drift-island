/**
 * 漂屿前端配置文件
 * 
 * 本地开发：保持注释状态即可，前端会自动请求同源的 /api
 * 线上部署：取消下面两行注释，填入你的 Render 后端地址
 */

// 部署到 GitHub Pages 时，把下面两行的注释去掉，改成你的 Render 地址
// window.DRIFT_API_BASE = 'https://your-app-name.onrender.com/api';
// window.DRIFT_WS_HOST = 'your-app-name.onrender.com';
