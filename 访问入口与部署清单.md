# 漂屿 (Drift Island) — 网站入口与部署清单

> 适用版本：`Lisben99/piaoyu-drift-island` 的 `main` 分支
> 生成日期：2026-08-10
> 本文是「四类网页 / 两套部署」的快速速查，详细运维见 `DEPLOYMENT.md`。

---

## 0. 一句话总览

整个项目只有 **两套部署**：

- **GitHub Pages（前端）** —— 给普通用户用的网页（大厅、注册）。
- **Render（后端）** —— Node/Express 服务，托管 API、WebSocket、`admin.html` 后台、健康检查。

所以你提到的这些「网页」对应关系如下（含管理员平台）：

| 你提到的 | 实际对应 | 网址 |
| --- | --- | --- |
| 前端网页 | GitHub Pages 上的用户前端 | `https://lisben99.github.io/piaoyu-drift-island/` |
| 后端网页 | Render 上的 API 接口 | `https://drift-island-api.onrender.com/api` |
| Render 部署的网页 | Render 服务（API + 后台 + 健康检查） | `https://drift-island-api.onrender.com` |
| GitHub 部署的网页 | GitHub Pages 站点 | `https://lisben99.github.io/piaoyu-drift-island/` |
| 管理员平台 | Render 托管的管理后台 `admin.html` | `https://drift-island-api.onrender.com/admin.html` |

---

## 1. 前端网页（GitHub Pages）

- **地址**：https://lisben99.github.io/piaoyu-drift-island/
- **包含的页面**（仓库根目录的静态 HTML，由 GitHub Pages 托管）：
  - `index.html` —— 大厅（漂流瓶广场、找 Bot 私聊的入口）
  - `register.html` —— 注册 / 登录
- **怎么连后端**：前端通过 `config.js` 里的 `window.DRIFT_API_BASE` 指向 Render 后端地址（`https://drift-island-api.onrender.com`）。
- **怎么更新**：改了 `index.html` / `register.html` / `config.js` 等前端文件 → 推送到 `main` → GitHub Pages 自动重新发布（通常 1–2 分钟生效）。

---

## 2. 后端网页 / 后端 API（Render）

- **API 基地址**：https://drift-island-api.onrender.com/api
- **WebSocket**：`wss://drift-island-api.onrender.com/ws?token=<用户token>`
- **后台管理页**：https://drift-island-api.onrender.com/admin.html
  - 管理员登录：用户名 `admin`，密码用你设置过的那个（源码里曾见默认 `yuanyi0318`，但 `DEPLOYMENT.md` 称已改；以线上为准，忘记了可用 `POST /api/admin/change-password` 改）。
  - 在这里做：开关机器人、配置 AI、卡密管理、查看数据等。
- **健康检查**：https://drift-island-api.onrender.com/api/health
  - 正常返回里包含 `sms_provider` / `moderation_provider` / `redeem_provider` 等字段；AI 接入后可看 `aiConfigured`。
- **技术栈**：Node.js 22 + Express + `ws`，入口 `server/src/index.js`。
- **注意**：Render 后端用 `express.static` 把仓库根的 `admin.html` 也托管了出来，所以后台是从 Render 访问的（不是 GitHub Pages）。

---

## 2.5 管理员平台（`admin.html`）— 单独说明

- **访问地址**：https://drift-island-api.onrender.com/admin.html
- **性质**：这是运营后台（不是给用户用的），从 Render 后端访问，不需要 GitHub Pages。
- **登录账号**：
  - 用户名：`admin`
  - 密码：源码里曾见默认 `yuanyi0318`，但 `DEPLOYMENT.md` 称已改过 —— **以线上为准**。忘记了可用 `POST /api/admin/change-password`（或后台内的改密入口）重置。
  - ⚠️ 上线后强烈建议改掉默认密码，避免被他人进后台乱改配置。
- **后台能做的事**（按卡片分区）：
  - **业务配置**：开关机器人引擎、AI 回复、机器人私聊（`enable_bot_private_chat` 需手动勾）、调 AI 温度/角色 Prompt 等。
  - **🤖 机器人管理**：看 `aiConfigured` / `aiProvider`（接好后标题显示「🤖 AI 已接入（硅基流动 SiliconFlow）」）、配各 Bot 的人设与私聊延迟。
  - **卡密管理 / 用户管理 / 数据看板**：发漂流币、查用户、看活跃数据等。
- **怎么更新后台**：改了 `admin.html` 或后端配置逻辑 → 推 `main` → Render 自动重新部署。

---

## 3. Render 部署的网页（部署侧）

- **控制台**：https://dashboard.render.com/ → 找到 Web Service **`drift-island-api`**（PostgreSQL 数据库 **`drift-island-db`**）。
- **蓝图文件**：仓库根 `render.yaml`，定义了数据库 + Web Service，`autoDeploy: true`（推 `main` 自动部署）。
- **重要环境变量**（Render → 服务 → Environment 设置）：
  | 变量 | 说明 | 状态 |
  | --- | --- | --- |
  | `DATABASE_URL` | 自动从 `drift-island-db` 注入 | 自动 |
  | `JWT_SECRET` | 自动生成 | 自动 |
  | `AI_API_KEY` | **硅基流动 Key**（你自己加的，不在 `render.yaml` 里） | 需手动填 |
  | `AI_PROVIDER` / `AI_BASE_URL` / `AI_MODEL` | 可不填，代码默认 硅基 + `Qwen/Qwen3-8B` | 可选 |
  | `SMS_PROVIDER` / `ALIYUN_SMS_*` | 阿里云短信（个人实名通道） | 按需 |
  | `MODERATION_PROVIDER` / `TENCENT_*` | 腾讯内容审核 | 按需 |
- **⚠️ 部署铁律**：Render 只监听 `main` 分支。所有改动必须 `git push origin main`，推 `master` 不会触发部署。
- **免费实例**：空闲约 15 分钟会休眠，首次访问需 ~30 秒冷启动，首次请求可能超时/404，**重试一次**即可。

---

## 4. GitHub 部署的网页（部署侧）

- **仓库**：https://github.com/Lisben99/piaoyu-drift-island （分支 `main`）
- **前端托管**：GitHub Pages，站点即上面的「前端网页」地址。
- **启用 / 检查**：仓库 → Settings → Pages → Source 选 `main` 分支（根目录）。
- **与 Render 的关系**：GitHub Pages 只放静态前端；所有数据、AI、登录都走 Render 后端，二者通过 `config.js` 里的 API 基地址连接。

---

## 5. 当前 AI 接入状态（2026-08-10）

- 默认服务商：**硅基流动 SiliconFlow**（OpenAI 兼容）。
- 默认模型：**`Qwen/Qwen3-8B`**（9B 以下永久免费档）。
- 启用方式：Render 里只填 `AI_API_KEY`（硅基 Key）。后台「业务配置 → 允许机器人私聊」勾上后，用户给 Bot 发消息即由 Qwen3-8B 自动回复。
- 后台「🤖 机器人管理」标题应显示：**🤖 AI 已接入（硅基流动 SiliconFlow）**。

---

## 6. 快速验证（部署后）

1. 打开 https://drift-island-api.onrender.com/api/health —— 确认服务在线。
2. 打开后台 https://drift-island-api.onrender.com/admin.html —— 确认「AI 已接入（硅基流动 SiliconFlow）」徽章。
3. 打开前端 https://lisben99.github.io/piaoyu-drift-island/ —— 大厅找 Bot 漂流瓶 →「和助手聊聊」→ 发一句话，3–8 秒后 Bot 自动回你。

---

## 7. 常见问题

| 现象 | 处理 |
| --- | --- |
| 前端打不开 / 样式旧 | GitHub Pages 有缓存，等 1–2 分钟或硬刷新 |
| 后台 404 / 接口不通 | Render 冷启动，重试一次；确认推的是 `main` |
| AI 徽章仍显示「未接入」 | 确认 Render 的 `AI_API_KEY` 已填并重新部署 |
| Bot 不回私聊 | 后台「允许机器人私聊」是否勾上并保存 |
| WebSocket 连不上 | 地址用 `wss://drift-island-api.onrender.com/ws?token=...` |

> 更完整的运维、短信/审核/支付对接、数据库备份见同仓库 `DEPLOYMENT.md`。
